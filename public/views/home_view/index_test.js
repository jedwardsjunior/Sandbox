'use strict';

describe('myApp.home_view module', function() {

  beforeEach(module('myApp.home_view'));

  describe('home_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeViewCtrl = $controller('HomeViewCtrl');
      expect(HomeViewCtrl).toBeDefined();
    }));

  });
});
