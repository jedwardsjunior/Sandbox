'use strict';

describe('myApp.result_view module', function() {

  beforeEach(module('myApp.result_view'));

  describe('result_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var resultViewCtrl = $controller('ResultViewCtrl');
      expect(resultViewCtrl).toBeDefined();
    }));

  });
});
